#include "main.h"

char *_strstr(char *haystack, char *needle)
{
		for (; *haystack != '\0'; haystack++)
				{
							char *l = haystack;
									char *p = needle;

													if (*p == '\0')
																	return (haystack);
														}


			return (0);
}

